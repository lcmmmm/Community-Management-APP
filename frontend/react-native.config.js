module.exports = {
    project: {
      ios: {},
      android: {}, // grouped into "project"
    },
    asset: [
      "./src/assets/fonts/",
      './src/assets/fonts/icons/'
    ] // stays the same
  };